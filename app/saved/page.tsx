"use client";
import{useEffect,useState}from"react";import{ArrowLeft,Bookmark,Play}from"lucide-react";import{useRouter}from"next/navigation";import{supabase}from"../../lib/supabase";
type Saved={id:string;url:string;title:string;like_count:number;created_at:string};
export default function SavedPage(){const router=useRouter();const[videos,setVideos]=useState<Saved[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{(async()=>{if(!supabase){router.replace("/auth");return}const{data:{session}}=await supabase.auth.getSession();if(!session){router.replace("/auth");return}const{saves,error:sErr}=await supabase.from("saved_videos").select("video_id,created_at").eq("user_id",session.user.id).order("created_at",{ascending:false});if(sErr){setError(sErr.message);setLoading(false);return}const ids=(saves||[]).map((x:any)=>x.video_id);if(!ids.length){setVideos([]);setLoading(false);return}const{data:rows,error:vErr}=await supabase.from("videos").select("id,url,title,like_count,created_at").in("id",ids);if(vErr){setError(vErr.message);setLoading(false);return}const order=new Map(ids.map((id:string,i:number)=>[id,i]));setVideos((rows||[]).sort((a:any,b:any)=>(order.get(a.id)??0)-(order.get(b.id)??0)));setLoading(false)})()},[router]);return <main className="savedPage"><style jsx global>{`.savedPage{min-height:100dvh;background:#070707;color:#fff;padding-bottom:30px}.savedHead{height:58px;display:flex;align-items:center;gap:14px;padding:0 15px;border-bottom:1px solid #ffffff12;position:sticky;top:0;background:#070707ee;backdrop-filter:blur(16px);z-index:5}.savedHead h1{font-size:18px;margin:0}.savedHead button{width:38px;height:38px;display:grid;place-items:center;background:none;color:#fff;border:0}.savedGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:2px;padding:2px}.savedCard{position:relative;aspect-ratio:9/16;overflow:hidden;background:#151515;border:0;padding:0}.savedCard video{width:100%;height:100%;object-fit:cover}.savedShade{position:absolute;inset:45% 0 0;background:linear-gradient(transparent,#0009)}.savedTitle{position:absolute;left:8px;right:8px;bottom:8px;color:#fff;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-align:left}.savedPlay{position:absolute;top:8px;left:8px;width:28px;height:28px;border-radius:50%;display:grid;place-items:center;background:#0008}.savedEmpty{min-height:70vh;display:grid;place-items:center;text-align:center;color:#888;padding:30px}.savedEmpty b{display:block;color:#fff;margin:12px 0 6px}.savedEmpty button{margin-top:16px;padding:10px 22px;border:0;border-radius:9px;background:#fff;color:#000;font-weight:800}.savedError{padding:40px 20px;text-align:center;color:#f66}`}</style><header className="savedHead"><button onClick={()=>router.back()}><ArrowLeft/></button><h1>我的收藏</h1></header>{loading?<div className="savedEmpty">加载收藏中…</div>:error?<div className="savedError">收藏加载失败：{error}</div>:videos.length?<section className="savedGrid">{videos.map(v=><button className="savedCard" key={v.id} onClick={()=>router.push(`/video/${v.id}`)}><video src={v.url} muted playsInline preload="metadata"/><span className="savedShade"/><span className="savedPlay"><Play size={14} fill="white"/></span><span className="savedTitle">{v.title||"星流视频"}</span></button>)}</section>:<div className="savedEmpty"><div><Bookmark size={48}/><b>还没有收藏</b><span>在首页收藏喜欢的视频，它们会一直保存在这里。</span><br/><button onClick={()=>router.push("/")}>去发现视频</button></div></div>}</main>}
