"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, Settings, LogOut, Plus, Heart, MessageCircle } from "lucide-react";
import { supabase } from "../../lib/supabase";

export default function ProfilePage(){
 const [session,setSession]=useState<any>(null); const [profile,setProfile]=useState<any>(null); const [videos,setVideos]=useState<any[]>([]); const [loading,setLoading]=useState(true);
 useEffect(()=>{(async()=>{if(!supabase)return; const {data:{session}}=await supabase.auth.getSession(); if(!session){location.href="/auth";return} setSession(session); const [{data:p},{data:v}]=await Promise.all([supabase.from("profiles").select("*").eq("id",session.user.id).maybeSingle(),supabase.from("videos").select("id,title,video_url,cover_url,like_count,comment_count,created_at").eq("user_id",session.user.id).eq("status","published").order("created_at",{ascending:false})]); setProfile(p);setVideos(v||[]);setLoading(false)})()},[]);
 async function logout(){await supabase?.auth.signOut();location.href="/"}
 if(loading)return <main className="pPage"><div className="pLoading">加载中…</div></main>;
 const name=profile?.display_name||profile?.username||session?.user?.email?.split("@")[0]||"星流用户";
 return <main className="pPage"><style jsx global>{`.pPage{min-height:100dvh;background:#050505;color:#fff;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif}.pTop{height:58px;display:flex;align-items:center;justify-content:space-between;padding:0 18px;border-bottom:1px solid #202020;position:sticky;top:0;background:rgba(5,5,5,.9);backdrop-filter:blur(14px);z-index:5}.pTop a,.pIcon{color:#fff}.pHead{max-width:720px;margin:auto;padding:28px 20px 22px}.pIdentity{display:flex;gap:16px;align-items:center}.avatar{width:76px;height:76px;border-radius:50%;object-fit:cover;background:#191919;border:1px solid #333}.pName{font-size:21px;font-weight:800}.pBio{color:#999;margin-top:6px;font-size:13px}.stats{display:flex;gap:30px;margin-top:20px}.stat b{display:block;font-size:19px}.stat span{font-size:12px;color:#888}.actions{display:flex;gap:10px;margin-top:20px}.btn{flex:1;height:42px;border-radius:12px;border:1px solid #333;background:#151515;color:#fff;font-weight:700;text-decoration:none;display:grid;place-items:center}.btn.primary{background:#fff;color:#000;border-color:#fff}.grid{max-width:720px;margin:auto;border-top:1px solid #222;display:grid;grid-template-columns:repeat(3,1fr);gap:2px}.tile{aspect-ratio:3/4;background:#111;position:relative;overflow:hidden;text-decoration:none;color:#fff}.tile img,.tile video{width:100%;height:100%;object-fit:cover}.tileMeta{position:absolute;bottom:7px;left:7px;right:7px;display:flex;gap:10px;font-size:11px;text-shadow:0 1px 5px #000}.empty{grid-column:1/-1;text-align:center;color:#777;padding:70px 20px}.pLoading{text-align:center;padding:100px;color:#777}@media(max-width:600px){.pHead{padding:24px 16px 18px}.grid{width:100%}}`}</style><header className="pTop"><Link href="/" aria-label="返回"><ArrowLeft size={22}/></Link><span>个人主页</span><div style={{display:'flex',gap:16}}><Link href="/settings" className="pIcon"><Settings size={20}/></Link><button className="pIcon" style={{background:'none',border:0}} onClick={logout}><LogOut size={20}/></button></div></header><section className="pHead"><div className="pIdentity"><img className="avatar" src={profile?.avatar_url||"/icon.svg"} alt="头像"/><div><div className="pName">{name}</div><div className="pBio">{profile?.bio||"记录生活，分享每一个瞬间。"}</div></div></div><div className="stats"><div className="stat"><b>{videos.length}</b><span>作品</span></div><div className="stat"><b>{profile?.followers_count||0}</b><span>粉丝</span></div><div className="stat"><b>{profile?.following_count||0}</b><span>关注</span></div><div className="stat"><b>{profile?.likes_count||0}</b><span>获赞</span></div></div><div className="actions"><Link className="btn primary" href="/upload"><Plus size={16}/> 发布作品</Link><Link className="btn" href="/settings">编辑资料</Link></div></section><section className="grid">{videos.length?videos.map(v=><Link className="tile" href={`/video/${v.id}`} key={v.id}>{v.cover_url?<img src={v.cover_url} alt=""/>:<video src={v.video_url} muted playsInline preload="metadata"/>}<div className="tileMeta"><span>♥ {v.like_count||0}</span><span>评论 {v.comment_count||0}</span></div></Link>):<div className="empty">还没有作品<br/><br/><Link href="/upload" style={{color:'#fff'}}>发布你的第一个视频 →</Link></div>}</section></main>;
}
