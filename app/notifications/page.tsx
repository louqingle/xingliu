"use client";
import{useEffect,useState}from"react";import{ArrowLeft,Heart,MessageCircle,UserPlus,Bell}from"lucide-react";import{supabase}from"../../lib/supabase";
type Notice={id:string;type:string;payload:any;created_at:string;read:boolean;actor:string;avatar:string};export default function NotificationsPage(){const[items,setItems]=useState<Notice[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{if(!supabase)return;const{data:{session}}=await supabase.auth.getSession();if(!session){location.href="/auth";return}const{data}=await supabase.from("notifications").select("id,type,payload,read,created_at,actor_id").eq("user_id",session.user.id).order("created_at",{ascending:false}).limit(100);const ids=[...new Set((data||[]).map((x:any)=>x.actor_id).filter(Boolean))];const{data:ps}=ids.length?await supabase.from("profiles").select("id,display_name,username").in("id",ids):{data:[]as any[]};setItems((data||[]).map((n:any)=>{const p=ps?.find((x:any)=>x.id===n.actor_id);return{id:n.id,type:n.type,payload:n.payload||{},created_at:n.created_at,read:n.read,actor:p?.display_name||p?.username||"有人",avatar:(p?.display_name||p?.username||"星").slice(0,1)}}));setLoading(false)})();},[]);async function markAll(){if(!supabase)return;const{data:{session}}=await supabase.auth.getSession();if(!session)return;await supabase.from("notifications").update({read:true}).eq("user_id",session.user.id);setItems(x=>x.map(n=>({...n,read:true})))}return <main className="noticePage"><style jsx global>{`.noticePage{min-height:100dvh;background:#070707;color:#fff}.noticeHead{height:58px;display:flex;align-items:center;justify-content:space-between;padding:0 15px;border-bottom:1px solid #ffffff12;position:sticky;top:0;background:#070707ee;backdrop-filter:blur(16px);z-index:5}.noticeHead h1{font-size:18px;margin:0}.noticeHead button{height:38px;min-width:38px;display:grid;place-items:center;color:#aaa}.noticeList{padding:0 10px}.noticeRow{display:flex;align-items:flex-start;gap:12px;padding:15px 6px;border-bottom:1px solid #ffffff09}.noticeRow.unreadNotice{background:#101010}.noticeBody{flex:1;line-height:1.5}.noticeBody b{margin-right:4px}.noticeBody small{display:block;color:#666;font-size:11px;margin-top:5px}.noticeBody p{margin:7px 0 0;color:#aaa;font-size:13px;background:#121212;border-radius:8px;padding:8px 10px}.noticeIcon{color:#888;margin-top:6px}.socialAvatar{width:50px;height:50px;flex:0 0 50px;border-radius:50%;background:linear-gradient(135deg,#fff,#777);color:#111;display:grid;place-items:center;font-weight:900;font-size:19px}.emptyState{min-height:70vh;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:#888}.emptyState b{color:#ddd}`}</style><header className="noticeHead"><button onClick={()=>history.back()}><ArrowLeft/></button><h1>通知</h1><button onClick={markAll}>全部已读</button></header>{loading?<div className="emptyState">正在加载…</div>:items.length?<div className="noticeList">{items.map(n=><div className={n.read?"noticeRow":"noticeRow unreadNotice"} key={n.id}><div className="socialAvatar">{n.avatar}</div><div className="noticeBody"><div><b>{n.actor}</b>{textFor(n.type)}</div><small>{timeAgo(n.created_at)}</small>{n.payload?.content&&<p>{n.payload.content}</p>}</div><span className="noticeIcon">{n.type==="like"?<Heart size={20}/>:n.type==="comment"?<MessageCircle size={20}/>:n.type==="follow"?<UserPlus size={20}/>:<Bell size={20}/>}</span></div>)}</div>:<div className="emptyState"><Bell size={48}/><b>暂无通知</b><span>点赞、评论、关注和私信都会出现在这里</span></div>}</main>}
function textFor(t:string){return t==="like"?" 赞了你的作品":t==="comment"?" 评论了你的作品":t==="follow"?" 关注了你":" 给你发来一条私信"}function timeAgo(s:string){const m=Math.floor((Date.now()-new Date(s).getTime())/60000);if(m<1)return"刚刚";if(m<60)return`${m}分钟前`;const h=Math.floor(m/60);if(h<24)return`${h}小时前`;const d=Math.floor(h/24);return d<7?`${d}天前`:new Date(s).toLocaleDateString("zh-CN")}
