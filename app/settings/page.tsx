"use client";

import { useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, KeyRound, LogOut, ShieldCheck, UserRound } from "lucide-react";
import { useRouter } from "next/navigation";
import { supabase } from "../../lib/supabase";

export default function SettingsPage() {
  const router = useRouter();
  const [email,setEmail]=useState(""); const [loading,setLoading]=useState(true); const [busy,setBusy]=useState(false); const [msg,setMsg]=useState("");
  useEffect(()=>{(async()=>{if(!supabase){router.replace("/auth");return}const {data}=await supabase.auth.getSession();if(!data.session){router.replace("/auth");return}setEmail(data.session.user.email||"");setLoading(false)})()},[router]);
  async function logout(scope:"local"|"global") { if(!supabase)return; setBusy(true);setMsg("");const {error}=await supabase.auth.signOut({scope});if(error){setMsg(error.message);setBusy(false);return}router.replace("/auth"); }
  async function resetPassword(){if(!supabase||!email)return;setBusy(true);setMsg("");const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/reset`});setMsg(error?`发送失败：${error.message}`:"重置密码邮件已发送，请检查邮箱");setBusy(false)}
  if(loading)return <main className="settings-page"><div className="settings-loading">加载中…</div></main>;
  return <main className="settings-page"><style jsx global>{`.settings-page{min-height:100dvh;background:#050505;color:#fff;padding:18px 16px 110px;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif}.settings-wrap{width:min(520px,100%);margin:0 auto}.settings-head{height:52px;display:flex;align-items:center;gap:14px}.settings-head button{width:40px;height:40px;border-radius:12px;background:#151515;color:#fff;display:grid;place-items:center}.settings-head strong{font-size:19px}.settings-hero{padding:26px 4px 22px}.settings-hero h1{font-size:30px;margin:0 0 7px;letter-spacing:-.8px}.settings-hero p{margin:0;color:#777;font-size:13px}.settings-card{background:#101010;border:1px solid #202020;border-radius:18px;overflow:hidden;margin-bottom:14px}.settings-row{width:100%;min-height:67px;display:flex;align-items:center;gap:13px;padding:13px 16px;text-align:left;border-bottom:1px solid #1d1d1d;color:#fff}.settings-row:last-child{border-bottom:0}.settings-icon{width:38px;height:38px;border-radius:12px;background:#191919;display:grid;place-items:center;color:#bbb;flex:none}.settings-copy{flex:1;min-width:0}.settings-copy strong{display:block;font-size:14px}.settings-copy span{display:block;color:#666;font-size:11px;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.settings-row b{font-size:12px;color:#777}.settings-msg{padding:12px 14px;background:#122219;color:#8ce0a4;border:1px solid #214b30;border-radius:12px;font-size:12px;margin-bottom:14px}.danger{color:#ff8e95!important}.danger .settings-icon{color:#ff8e95}.settings-loading{min-height:100dvh;display:grid;place-items:center;color:#777}`}</style><div className="settings-wrap"><header className="settings-head"><button onClick={()=>router.back()}><ArrowLeft size={20}/></button><strong>账号与安全</strong></header><section className="settings-hero"><h1>账号安全</h1><p>管理你的登录方式、密码和设备会话</p></section>{msg&&<div className="settings-msg"><CheckCircle2 size={14} style={{verticalAlign:"-2px",marginRight:5}}/>{msg}</div>}<section className="settings-card"><div className="settings-row"><div className="settings-icon"><UserRound size={18}/></div><div className="settings-copy"><strong>登录邮箱</strong><span>{email||"未设置"}</span></div><b>已绑定</b></div><button className="settings-row" onClick={resetPassword} disabled={busy}><div className="settings-icon"><KeyRound size={18}/></div><div className="settings-copy"><strong>修改密码</strong><span>发送安全邮件后设置新密码</span></div><b>›</b></button></section><section className="settings-card"><div className="settings-row"><div className="settings-icon"><ShieldCheck size={18}/></div><div className="settings-copy"><strong>安全状态</strong><span>Supabase Session 持久化登录</span></div><b>正常</b></div><button className="settings-row" onClick={()=>logout("local")} disabled={busy}><div className="settings-icon"><LogOut size={18}/></div><div className="settings-copy"><strong>退出当前设备</strong><span>只退出当前手机/浏览器</span></div><b>›</b></button><button className="settings-row danger" onClick={()=>logout("global")} disabled={busy}><div className="settings-icon"><LogOut size={18}/></div><div className="settings-copy"><strong>退出所有设备</strong><span>立即结束其他设备的登录会话</span></div><b>›</b></button></section></div></main>;
}
